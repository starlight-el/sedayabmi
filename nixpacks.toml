[phases.setup]
nixPkgs = ["php82", "php82Extensions.intl", "php82Extensions.mbstring", "php82Extensions.curl", "php82Extensions.gd", "php82Extensions.pdo"]

[start]
cmd = "php -S 0.0.0.0:$PORT -t public"